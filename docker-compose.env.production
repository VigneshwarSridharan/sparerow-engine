# Sparerow Engine — Production Environment
# Copy this to .env and fill in actual values

# Domain & SSL (Let's Encrypt)
# Comma-separated list to cover multiple hostnames on one cert, e.g. the apex
# and www (yourdomain.com,www.yourdomain.com). The first entry names the cert.
LETSENCRYPT_DOMAIN=yourdomain.com,www.yourdomain.com
LETSENCRYPT_EMAIL=admin@yourdomain.com

# Database
POSTGRES_USER=strapi
POSTGRES_PASSWORD=change_this_to_secure_password
POSTGRES_DB=strapi_spare

# Storefront
# Use relative path so frontend makes requests to same domain
VITE_STOREFRONT_GRAPHQL_ENDPOINT=/graphql

# Observability (all free-tier, all optional — leave blank to disable)
VITE_SENTRY_DSN=
VITE_POSTHOG_KEY=
VITE_POSTHOG_HOST=https://us.i.posthog.com

# Optional: exposing backend port for debugging (remove in final production)
# STRAPI_PORT=1337
